"""WebDataset-backed speech dataset (placeholder for Phase 3).

This module will implement a streaming :class:`SpeechDataset` backed by
WebDataset ``.tar`` shards, compatible with the output of
``src/pipeline/package.py``.

Implementation deferred to Phase 3. See ``docs/DATALOADER_DESIGN.md``.
"""
